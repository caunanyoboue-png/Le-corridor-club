{
  "buildCommand": "pnpm --filter shared build && pnpm --filter api build && pnpm --filter web build",
  "installCommand": "pnpm install --frozen-lockfile",
  "env": {
    "NODE_ENV": "production"
  },
  "projects": {
    "web": {
      "name": "le-corridor-club-web",
      "alias": ["le-corridor-club-web"],
      "root": "apps/web",
      "buildCommand": "pnpm --filter shared build && pnpm --filter web build",
      "outputDirectory": "dist",
      "framework": "vite"
    },
    "api": {
      "name": "le-corridor-club-api",
      "alias": ["le-corridor-club-api"],
      "root": "apps/api",
      "buildCommand": "pnpm --filter shared build && pnpm --filter api build",
      "outputDirectory": "dist",
      "functions": {
        "api/index.ts": {
          "runtime": "nodejs20.x",
          "memory": 512,
          "maxDuration": 60
        }
      }
    }
  }
}
